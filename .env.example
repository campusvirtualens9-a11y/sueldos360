# Supabase — configuración pública (puede ir en frontend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — solo servidor, NUNCA en frontend
SUPABASE_SERVICE_ROLE_KEY=

# Base de datos — conexión directa (solo para migraciones locales)
DATABASE_URL=

# GitHub — solo si se necesita para tareas de repositorio
# GITHUB_TOKEN=
